import React from 'react';
import store from '../../config/store'
import './style.css';
import { connect } from 'react-redux';

export default function handleUpdate(player_stats) {


 function getMenuStats(type) {
        
    }
   }